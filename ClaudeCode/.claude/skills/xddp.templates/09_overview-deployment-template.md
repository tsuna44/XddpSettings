---
version: "1.0.0"
last-updated-cr: "{CR}"
last-verified-cr: "{CR}"
source: spo
repo: "{repo}"
---

# ハードウェア構成・デプロイ構成 — {repo}

**リポジトリ:** {repo}  
**最終更新CR:** {CR}  

> **生成条件:** SPO サマリーに「ハードウェア構成・デプロイ構成」セクションが存在する場合のみ生成する。
> Webシステム: コンテナ/インフラ構成（XDDP 管轄外の場合は生成省略可）。
> 組み込み: ハードウェア構成図・タスク配置・メモリマップ・ペリフェラル割り当て（組み込みでは必須ビューとなることが多い）。
> プロジェクト種別に応じて生成要否を判断する。

---

## 1. 文書概要

| 項目 | 内容 |
|---|---|
| 対象リポジトリ | {repo} |
| システム種別 | Webシステム／組み込み／その他 |

---

## 2a. デプロイ構成（Webシステム）

> コンテナ構成・デプロイ先・インフラ構成を示す。
> 組み込みシステムの場合はこのセクションを削除し、Section 2b を使用する。

```mermaid
graph TB
    subgraph Production
        {ServiceA}["{サービスA\nコンテナ}"]
        {ServiceB}["{サービスB\nコンテナ}"]
        {DB}[("{データベース}")]
        {ServiceA} --> {DB}
        {ServiceB} --> {DB}
    end
    {LoadBalancer}["ロードバランサー"] --> {ServiceA}
    {LoadBalancer} --> {ServiceB}
```

| コンポーネント | 種別 | デプロイ先 | 説明 |
|---|---|---|---|
| {ServiceA} | コンテナ | {デプロイ環境} | {説明} |

---

## 2b. ハードウェア構成（組み込みシステム）

> ハードウェア構成図・タスク配置・メモリマップ・ペリフェラル割り当てを示す。
> Webシステムの場合はこのセクションを削除し、Section 2a を使用する。

```mermaid
graph TB
    subgraph MCU
        {TaskA}["タスクA\n{優先度}"]
        {TaskB}["タスクB\n{優先度}"]
        {SharedMem}[("{共有メモリ}")]
        {TaskA} --> {SharedMem}
        {TaskB} --> {SharedMem}
    end
    subgraph Peripheral
        {UART}["UART"]
        {GPIO}["GPIO"]
    end
    {TaskA} --> {UART}
    {TaskB} --> {GPIO}
```

| タスク名 | 優先度 | 周期 | スタックサイズ | 担当モジュール |
|---|---|---|---|---|
| {TaskA} | {優先度} | {周期ms} | {バイト} | {モジュール名} |

**メモリマップ:**

| アドレス | サイズ | 用途 |
|---|---|---|
| 0x{ADDRESS} | {サイズ} | {用途} |

**ペリフェラル割り当て:**

| ペリフェラル | 種別 | 担当タスク・モジュール | 設定 |
|---|---|---|---|
| {UART1} | シリアル通信 | {TaskA} | {ボーレート等} |

---

## 3. 気づき・提案メモ

| # | 種別 | 内容 | 対応方針 |
|---|------|------|----------|
| 1 | 修正点／改善案／懸念／質問 | {内容} | 今回対応／次回CR／保留／却下 |

---

## 4. 変更履歴

| バージョン | CR | 日付 | 変更内容 |
|---|---|---|---|
| 1.0.0 | {CR} | {YYYY-MM-DD} | 初版作成（SPO から生成） |
