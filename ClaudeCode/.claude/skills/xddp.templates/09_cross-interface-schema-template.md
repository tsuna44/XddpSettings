---
version: "1.0.0"
last-updated-cr: "{CR}"
last-verified-cr: "{CR}"
source: spo
interface: "{interface-kebab}"
provider: "{provider-repo}"
---

# インタフェーススキーマ定義 — {interface-kebab}

**インタフェース:** {interface-kebab}  
**提供リポジトリ:** {provider-repo}  
**最終更新CR:** {CR}  

> 気づきメモは `spec.md` に記録してください（このファイルには気づきメモセクションなし）。
> スキーマ変更の振る舞い・影響は `spec.md` を参照してください。

---

## 1. 文書概要

| 項目 | 内容 |
|---|---|
| インタフェース種別 | HTTP API／関数API／バス通信／IPC・共有メモリ |
| スキーマバージョン | 1.0.0 |

---

## 2. データ定義

> インタフェース種別に応じて以下の形式を選択し、不要な節を削除する。

### 2a. HTTP API（リクエスト・レスポンス）

**リクエスト本体:**

| フィールド | 型 | 必須 | 説明 |
|---|---|:---:|---|
| {フィールド名} | {型} | ○/— | {説明} |

```json
{
  "{field1}": "{value1}",
  "{field2}": 123
}
```

**レスポンス本体（成功）:**

| フィールド | 型 | 説明 |
|---|---|---|
| {フィールド名} | {型} | {説明} |

```json
{
  "id": 1,
  "{field1}": "{value1}"
}
```

**エラーレスポンス:**

| ステータスコード | エラーコード | 原因 |
|---|---|---|
| 400 | VALIDATION_ERROR | リクエストバリデーション失敗 |
| 404 | {RESOURCE}_NOT_FOUND | リソース不存在 |

### 2b. 関数API（引数・戻り値型定義）

```c
typedef struct {
    {type} {field};   /* {説明} */
} {StructName}_t;
```

| フィールド | 型 | オフセット | 説明 |
|---|---|---|---|
| {field} | {型} | {バイト位置} | {説明} |

### 2c. バス通信（フレームフォーマット・ビット定義）

| バイト | ビット | フィールド名 | 型 | 説明 | 値域 |
|---|---|---|---|---|---|
| 0 | 7:0 | {フィールド名} | uint8 | {説明} | 0–255 |

### 2d. IPC・共有メモリ（構造体定義）

```c
typedef struct {
    volatile {type} {field};   /* {説明} */
} {SharedStructName}_t;
```

---

## 3. 変更履歴

| バージョン | CR | 日付 | 変更内容 |
|---|---|---|---|
| 1.0.0 | {CR} | {YYYY-MM-DD} | 初版作成 |
