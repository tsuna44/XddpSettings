# 要求書

**文書番号：** REQ-{YYYY}-{NNN}  
**タイトル：** {変更タイトル}  
**作成日：** {YYYY-MM-DD}  
**作成者：** {氏名}  
**版数：** 1.0

---

> **記入の目安：** 全項目を厳密に埋める必要はない。わかる範囲・言える範囲を自由な文章で書けばよい。
> 詳細な影響範囲やソースコード上のモジュール構成は工程4（スペックアウト／母体調査）で特定し、
> 要求の分類・優先度整理は工程2（分析）・工程3（変更要求仕様書作成）で補われる。
> この文書はまず「何を・なぜ変えたいか」を伝えることを優先してよい。
>
> **関連資料・要求内容自体を複数ファイルに分けたい場合：** 状態遷移マトリクス等の関連ドキュメントや、
> 要求内容自体を複数ファイルに分けて書きたい場合は、本文への転記や特別なリンク記法は不要。
> `{CR}/01_requirements/` フォルダ（`/xddp.01.init` 実行後に作成される）に追加のMarkdownファイル
> として置くだけでよい。同じフォルダ内のファイルは工程2（分析）ですべて自動的に読み込まれるため、
> このファイル側でファイル名や相対パスに言及する必要はない（言及してもよいが、それは人が読むときの
> 手がかり用）。ただし追加ファイルの名前を `REQ-{CR}.md` と完全に一致させないこと（`/xddp.01.init` を
> 再実行した場合、コピー処理で意図せず上書きされる可能性がある）。`/xddp.01.init` 実行時点で
> まだ手元にない場合は、後から追加してもよい。

---

## 1. 背景と目的

### 1.1 背景

{変更が必要になった経緯・課題・問題点を記述する}

### 1.2 目的

{この変更によって何を達成したいかを記述する（＝この変更で達成したいゴール・成果）}

---

## 2. 用語定義（任意）

> プロジェクト固有の専門用語がある場合のみ記載する。特になければ本セクションは
> 「該当なし」と書くか、削除して次のセクションへ進んでよい。

| 用語 | 定義 |
|------|------|
| {用語1} | {定義1} |

---

## 3. 要求・要望

> 1.2「目的」が変更のゴール（なぜ行うか）であるのに対し、本セクションは
> そのゴールを達成するために実現してほしいこと・困っていること（何を行うか）を記述する。
> 実現してほしいこと・困っていることを自由に書けばよい。USDMのUR（ユーザ要求）／SR（システム要求）／SP（仕様）
> への分類は工程2（分析）でAIが内容から判断して行う。
> 具体的な値や条件など仕様寄りの内容を書いてしまっても構わない。

### 機能要求

- {要求・要望の内容}
- {要求・要望の内容}

### 非機能要求（該当する場合のみ）

- {性能・信頼性・保守性等について要望があれば記述する}

---

## 4. テスト要求

### 4.1 単体テスト

> 本セクションの単体テスト判定方針は、プロジェクト共通として `xddp.config.md` の設定に従う。
> 具体的な数値・基準は同ファイルを参照すること（本文中に数値を重複して記載しない）。
> 本セクションはCRごとの記入不要（上記2文が全CR共通の確定内容。変更する場合はテンプレート自体を編集する）。

### 4.2 結合テスト

> テストしたい内容を自由に記述する。以下は参考の観点であり、必要な項目だけ触れればよい
> （すべて埋める必要はない。詳細に書きたい場合は項目を増やしてよい）。
> 参考の観点：テスト対象、確認したい組み合わせ・シナリオ、合否基準、環境・自動化方針（必要な場合のみ）

{テストしたい内容を記述する}

---

## 5. 既知の影響部位（任意）

> 実装上、影響する箇所（モジュール・機能・ファイル等）がすでにわかっている場合のみ記述する。
> ここに書いた内容はメモとして扱われ、工程4（スペックアウト）で改めて調査・確認される
> （正確性を保証する必要はない）。わからない場合は本セクションを省略してよい。

{わかる範囲で記述する（例：モジュール名、コンポーネント名、インタフェース名 等）}

---

## 6. 制約条件

> なければ「なし」でよい。既存の状態遷移マトリクス等、制約となる既存資料がある場合は、
> 冒頭の「関連資料・要求内容自体を複数ファイルに分けたい場合」に従ってファイルを配置し、
> ここではファイル名に触れる程度でよい。

- {技術的制約}
- {運用上の制約}
- {スケジュール・予算等の制約}

---

## 7. 未決事項

> なければ「なし」でよい。

- [ ] {未決の内容}（期限：{YYYY-MM-DD、不明なら省略可}）

---

## 8. 変更履歴

| 版数 | 日付 | 変更者 | 変更内容 |
|------|------|--------|----------|
| 1.0 | {YYYY-MM-DD} | {氏名} | 初版作成 |
