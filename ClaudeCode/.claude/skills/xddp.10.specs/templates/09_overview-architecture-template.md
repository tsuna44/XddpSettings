---
version: "1.0.0"
last-updated-cr: "{CR}"
last-verified-cr: "{CR}"
source: spo
repo: "{repo}"
has_insights: true
---

# リポジトリアーキテクチャ図 — {repo}

**リポジトリ:** {repo}  
**最終更新CR:** {CR}  

> ⚠️ このファイルは複数 CR にわたってマージ更新される共有ファイルです。
> 今回 CR で調査したモジュールのみ更新し、未調査モジュールは既存の記述を保持します。
> 並行 CR が存在する場合は xddp.10.specs を逐次実行してください（同時更新リスク）。

---

## 1. 文書概要

| 項目 | 内容 |
|---|---|
| 対象リポジトリ | {repo} |
| 登録モジュール数 | {N} モジュール |
| 最終フルスキャン CR | {CR}（定期棚卸し推奨） |

---

## 2. アーキテクチャ図（コンポーネント図）

> リポジトリ内モジュール・コンポーネントの構成と依存方向を示す。
> SPO サマリー「モジュール依存・コンポーネント構成」セクションから取得する。
> **更新方針:** 今回 CR の SPECOUT_MODULES に含まれるモジュールのみ更新。未調査モジュールは保持。

```mermaid
graph TB
    subgraph {repo}
        {ModuleA}["モジュールA\n({主要ファイル})"]
        {ModuleB}["モジュールB\n({主要ファイル})"]
        {ModuleC}["モジュールC\n({主要ファイル})"]
        {ModuleA} --> {ModuleB}
        {ModuleA} --> {ModuleC}
    end
```

---

## 3. モジュール一覧

| モジュール名 | 主要ファイル | 責務 | 最終確認CR |
|---|---|---|---|
| {module-kebab} | {主要ファイルパス} | {モジュールの責務} | {CR} |

---

## 4. 依存方向説明

| 依存元 | 依存先 | 依存種別 | 説明 |
|---|---|---|---|
| {ModuleA} | {ModuleB} | 関数呼び出し／イベント／共有メモリ | {依存の説明} |

---

## 5. 気づき・提案メモ

> アーキテクチャ改善提案・モジュール名ドリフト候補等を記録する。

| # | 種別 | 内容 | 対応方針 |
|---|------|------|----------|
| 1 | 修正点／改善案／懸念／質問 | {内容} | 今回対応／次回CR／保留／却下 |

---

## 6. 変更履歴

| バージョン | CR | 日付 | 変更内容 |
|---|---|---|---|
| 1.0.0 | {CR} | {YYYY-MM-DD} | 初版作成（SPO から生成） |
