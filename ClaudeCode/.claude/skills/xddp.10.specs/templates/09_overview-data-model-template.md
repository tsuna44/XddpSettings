---
version: "1.0.0"
last-updated-cr: "{CR}"
last-verified-cr: "{CR}"
source: ai-inferred
repo: "{repo}"
has_insights: true
---

# データモデル — {repo}

**リポジトリ:** {repo}  
**最終更新CR:** {CR}  

> ⚠️ このファイルは複数 CR にわたってマージ更新される共有ファイルです。
> 並行 CR が存在する場合は xddp.10.specs を逐次実行してください（同時更新リスク）。
> `source: ai-inferred` は AI がマージ・推定した情報を含むことを示します。
> SPO 実測で確認できた場合は人手で `source: spo` に変更してください。

---

## 1. 文書概要

| 項目 | 内容 |
|---|---|
| 対象リポジトリ | {repo} |
| データ種別 | RDB（ER図）／組み込み（構造体依存関係図）／その他 |
| 最終フルスキャン CR | {CR}（定期棚卸し推奨） |

---

## 2. データモデル図

> **RDB を持つシステム:** ER 図（mermaid erDiagram）で主要エンティティ・属性・リレーションを示す。
> **組み込み・非RDBシステム:** 主要データ構造体・共有バッファ・グローバル変数の定義と依存関係をクラス図またはテキスト形式で示す。
> SPO サマリー「データモデル（エンティティ関連図・データ構造定義）」セクションから取得する。
> **更新方針:** 今回 SPO §4.3 に記述されているエンティティ・構造体のみ更新。非記載は保持。

```mermaid
erDiagram
    {ENTITY_A} {
        int id PK
        string name
        datetime created_at
    }
    {ENTITY_B} {
        int id PK
        int entity_a_id FK
        string value
    }
    {ENTITY_A} ||--o{ {ENTITY_B} : "{関係}"
```

---

## 3. エンティティ・構造体一覧

| 名前 | 種別 | 説明 | 確認状態 |
|---|---|---|---|
| {ENTITY_A} | エンティティ／構造体 | {説明} | 確認済み／（推定）／（要確認） |

---

## 4. 気づき・提案メモ

> データモデル見直し候補・「（要確認）」注記の未解決エントリを記録する。

| # | 種別 | 内容 | 対応方針 |
|---|------|------|----------|
| 1 | 修正点／改善案／懸念／質問 | {内容} | 今回対応／次回CR／保留／却下 |

---

## 5. 変更履歴

| バージョン | CR | 日付 | 変更内容 |
|---|---|---|---|
| 1.0.0 | {CR} | {YYYY-MM-DD} | 初版作成（SPO §4.3 から生成） |
