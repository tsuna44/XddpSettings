# 変更設計書

**文書番号：** CHD-{CR番号}  
**対象CR：** {CR番号}  
**タイトル：** {変更タイトル}  
**作成日：** {YYYY-MM-DD}  
**作成者：** AI（担当エージェント名）  
**版数：** 1.0

> このファイルは1つのUR（またはそのバッチ）の内容ファイルである。リポジトリ全体の一覧は
> インデックスファイル（`CHD-{CR番号}.md`）を参照すること。
> 対象範囲のSPがこのリポジトリに変更を持たない場合（バッチ内の全SPが他リポジトリの担当）、
> 第3章以降を省略し「対象範囲のSPはこのリポジトリに変更を伴わない」と記載してよい。

---

## 1. 変更概要

| 項目 | 内容 |
|------|------|
| 対象範囲 | {UR-ID}（バッチ {N}/{M}。単一バッチの場合は省略） |
| 採用方式 | {方式検討メモの採用案} |
| 変更ファイル数 | {件} |
| 参照資料 | CRS-{CR番号}、SPO-{CR番号}、DSN-{CR番号}、CHD-{CR番号}.md（インデックス） |

---

## 2. 変更対象ファイル一覧

> ファイル単位での変更概要。詳細は第3章（仕様単位）を参照。  
> 単一リポジトリの場合は「リポジトリ」列を省略してよい。

| # | リポジトリ | ファイルパス | 変更種別 | 対応SP番号 |
|---|-----------|------------|----------|-----------|
| 1 | {リポジトリ名（単一リポジトリは省略可）} | {パス} | 追加／変更／削除 | SP-001 |

---

## 3. 詳細設計

> **仕様（SP）単位**で変更内容を記述する。  
> 1つのSPが複数ファイルにまたがる場合は、そのSP節内でファイルごとに記述する。  
> 変更のないファイルはこの章に記載しない。  
> **実装コードはここに書かない。** コーダーが設計仕様から実装する。

### 3.1 SP-{番号}：{仕様名}

> {この仕様で何を実現するかを1〜2行で説明}

**変更ファイル：** `{ファイルパス}`（追加／変更／削除）  
**リポジトリ：** `{リポジトリ名}`（単一リポジトリの場合は省略可）

#### Before 設計

> 現状のインタフェース定義・データ構造・処理フロー概要。  
> SPO に詳細が記載済みの場合は「SPO §{セクション番号} 参照」と記載してよい。

| 種別 | 名称 | 定義 |
|------|------|------|
| 関数/手続き/コマンド/ハンドラ | `{名称}` | `({入力}) → {出力}` |
| データ構造/レジスタ/メッセージ/設定値 | `{名称}` | {フィールド・ビット・値域概要} |

```mermaid
%% 現状の構造・フロー（変化が分かりにくい場合のみ記載。不要なら削除）
%% シーケンス図・コンポーネント図・状態図など適切な図種別を選ぶ
```

#### After 設計

> 変更後のインタフェース定義・データ構造・処理フロー概要。

| 種別 | 名称 | 定義 |
|------|------|------|
| 関数/手続き/コマンド/ハンドラ | `{名称}` | `({入力}) → {出力}` |
| データ構造/レジスタ/メッセージ/設定値 | `{名称}` | {フィールド・ビット・値域概要} |

```mermaid
%% 変更後の構造・フロー
```

#### 変更仕様

- {変更点1：何をどう変えるか（理由込み）}
- {変更点2}

**制約・前提条件：**
- {実装時に守るべき制約（タイミング要件・スレッド安全性・エラー処理方針・リソース上限等）}

**実装ガイド（任意）：**
- {コーダーが判断に迷いそうな点を補足する。実装コードは書かない}

---

### 3.2 SP-{番号}：{仕様名}（複数ファイルにまたがる例）

> {この仕様で何を実現するかを1〜2行で説明}

**変更ファイル：** `{ファイルパス1}`、`{ファイルパス2}`  
**リポジトリ：** `{リポジトリ名1}`、`{リポジトリ名2}`（単一リポジトリの場合は省略可）

#### `{ファイルパス1}` の Before/After 設計

**Before：**

| 種別 | 名称 | 定義 |
|------|------|------|
| {種別} | `{名称}` | {定義} |

**After：**

| 種別 | 名称 | 定義 |
|------|------|------|
| {種別} | `{名称}` | {定義} |

**変更仕様：**
- {変更点1}

#### `{ファイルパス2}` の Before/After 設計

**Before：**

| 種別 | 名称 | 定義 |
|------|------|------|
| {種別} | `{名称}` | {定義} |

**After：**

| 種別 | 名称 | 定義 |
|------|------|------|
| {種別} | `{名称}` | {定義} |

**変更仕様：**
- {変更点1}

---

## 4. トレーサビリティマトリクス

> 仕様IDと変更箇所（ファイル・モジュール・シンボル）の対応を一覧化する。

| 仕様ID | 仕様名 | 変更ファイル | 変更シンボル（関数・構造体・定数等） |
|--------|--------|------------|--------------------------------------|
| SP-001 | {仕様名} | {ファイルパス} | {シンボル名} |

---

## 5. データ設計（該当する場合）

### 5.1 データ構造の変更

**Before：**

| フィールド/ビット | 型/幅 | 意味・値域 |
|-----------------|-------|----------|
| {名称} | {型} | {説明} |

**After：**

| フィールド/ビット | 型/幅 | 意味・値域 |
|-----------------|-------|----------|
| {名称} | {型} | {説明} |

### 5.2 移行手順・初期状態定義

> データ構造変更に伴う移行手順・初期状態の設定方法を記述する。ドメインに応じた内容を記述すること。
> RDB: マイグレーションスクリプト・デフォルト値設定・データコンバート手順。
> 組み込み: フラッシュ書き込み手順・EEPROM初期化・NVMリセット処理・ブートローダー更新手順等。
> 業務システム: 移行バッチ・既存データのコンバート・初期投入データ。
> 該当なしの場合は「対象外」と記載。

{移行手順・初期状態定義を記述}

---

## 6. インタフェース設計（該当する場合）

> 外部から観測可能なインタフェースの変更を一覧化する。
> ドメインに応じて適切な種別を使用すること（API・関数・プロトコル・バスI/F・割り込みベクタ等）。

| 種別 | 名称 | 変更内容 | breaking |
|------|------|----------|---------|
| {API/関数/プロトコル/バスI/F等} | `{名称}` | 追加／変更（仕様: {概要}）／削除 | true／false |

---

## 7. 確認項目（テスト観点）

> テスト設計の入力として使用する。仕様IDと対応させて記述する。

| # | 確認内容 | 確認種別 | 対応SP番号 |
|---|----------|----------|-----------|
| 1 | {確認したい動作・条件} | 正常系／異常系／境界値 | SP-001 |

---

## 8. 気づき・提案メモ

> 作成・レビュー中に気づいた修正すべき内容・改善案・懸念事項を自由に記録する。  
> 現在のCRスコープ外の内容も記載可。次のCR起票・バックログの入力として活用する。

| # | 種別 | 内容 | 対応方針 |
|---|------|------|----------|
| 1 | 修正点／改善案／懸念／質問 | {内容} | 今回対応／次回CR／保留／却下 |

---

## 9. 変更履歴

| 版数 | 日付 | 変更者 | 変更内容 |
|------|------|--------|----------|
| 1.0 | {YYYY-MM-DD} | AI | 初版作成 |
| 1.1 | {YYYY-MM-DD} | AI | 不具合修正を反映（テストNGフィードバック） |
