# モジュールカタログ — {repo}

**リポジトリ：** {repo}（`{repo_path}`）  
**生成日：** {YYYY-MM-DD}  
**生成スキル：** /xddp.codemap  
**言語：** {language}

> **更新方法：** `/xddp.codemap {repo}` を再実行すると上書き再生成される。手動修正後に再実行すると変更が失われる。
> **モジュール数：** {N} モジュール

---

## 1. モジュール依存グラフ

```mermaid
graph LR
  {module_a}["{module_a}/"] --> {module_b}["{module_b}/"]
```

---

## 2. モジュール一覧

### {module-name}/ — {役割一行説明}

- **ディレクトリ：** `{path}`
- **主要シンボル：** `{symbol1}`, `{symbol2}`, ...
- **依存先モジュール：** `{dep1}`, `{dep2}`（なし）
- **被依存元モジュール：** `{rdep1}`, `{rdep2}`（なし）

---

## 3. シンボル索引

> specout の BFS 優先度計算でシンボル→モジュール逆引きに使用する。

| シンボル名 | モジュールディレクトリ |
|---|---|
| `{symbol1}` | `{module_dir}` |

---

## 4. 変更履歴

| 日付 | 変更内容 |
|------|----------|
| {YYYY-MM-DD} | 初版生成（/xddp.codemap） |
