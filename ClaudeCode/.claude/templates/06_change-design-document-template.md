# 変更設計書

**文書番号：** CHD-{CR番号}  
**対象CR：** {CR番号}  
**タイトル：** {変更タイトル}  
**作成日：** {YYYY-MM-DD}  
**作成者：** AI（担当エージェント名）  
**版数：** 1.0

---

## 1. 変更概要

| 項目 | 内容 |
|------|------|
| 採用方式 | {方式検討メモの採用案} |
| 変更ファイル数 | {件} |
| 参照資料 | CRS-{CR番号}、SPO-{CR番号}、DSN-{CR番号} |

---

## 2. 変更対象ファイル一覧

> ファイル単位での変更概要。詳細は第3章（仕様単位）を参照。  
> 単一リポジトリの場合は「リポジトリ」列を省略してよい。

| # | リポジトリ | ファイルパス | 変更種別 | 対応SP番号 |
|---|-----------|------------|----------|-----------|
| 1 | {リポジトリ名（単一リポジトリは省略可）} | {パス} | 追加／変更／削除 | SP-001 |

---

## 3. 詳細設計

> **仕様（SP）単位**で変更内容を記述する。  
> 1つのSPが複数ファイルにまたがる場合は、該当ファイルごとにBefore/Afterを記述する。  
> 変更のないファイルはこの章に記載しない。

### 3.1 SP-{番号}：{仕様名}

> {この仕様で何を実現するかを1〜2行で説明}

**リポジトリ：** `{リポジトリ名}`（単一リポジトリの場合は省略可）  
**変更ファイル：** `{ファイルパス}`

#### `{ファイルパス}`

**Before（変更前）：**

```{言語}
{変更前のコード。新規追加の場合は「（新規追加）」と記載}
```

**After（変更後）：**

```{言語}
{変更後のコード}
```

**変更箇所の説明：**
- {変更点1}
- {変更点2}

**図（該当する場合）：**

```mermaid
%% クラス構造・呼び出し関係・シーケンス等、変更内容に適した図種別を選んで記述する
%% 図が不要な場合はこのブロックを削除する
```

---

### 3.2 SP-{番号}：{仕様名}（複数ファイル／複数リポジトリにまたがる例）

> {この仕様で何を実現するかを1〜2行で説明}

**リポジトリ：** `{リポジトリ名1}`、`{リポジトリ名2}`（単一リポジトリの場合は省略可）  
**変更ファイル：** `{ファイルパス1}`、`{ファイルパス2}`

#### `{ファイルパス1}`

**Before（変更前）：**

```{言語}
{変更前のコード}
```

**After（変更後）：**

```{言語}
{変更後のコード}
```

**変更箇所の説明：**
- {変更点1}

#### `{ファイルパス2}`

**Before（変更前）：**

```{言語}
{変更前のコード}
```

**After（変更後）：**

```{言語}
{変更後のコード}
```

**変更箇所の説明：**
- {変更点1}

---

## 4. トレーサビリティマトリクス

> 仕様IDと変更箇所（ファイル・関数）の対応を一覧化する。

| 仕様ID | 仕様名 | 変更ファイル | 変更関数・クラス・箇所 |
|--------|--------|------------|----------------------|
| SP-001 | {仕様名} | {ファイルパス} | {関数名・クラス名} |

---

## 5. データ設計（該当する場合）

### 5.1 データ構造の変更

**Before：**
```
{変更前のデータ構造・スキーマ}
```

**After：**
```
{変更後のデータ構造・スキーマ}
```

### 5.2 マイグレーション・初期データ

{必要な場合に記述}

---

## 6. インタフェース設計（該当する場合）

| 種別 | 名称 | 変更内容 |
|------|------|----------|
| API | {エンドポイント} | 追加／変更／削除 |
| 関数 | {関数名} | シグネチャ変更等 |

---

## 7. 確認項目（テスト観点）

> テスト設計の入力として使用する。仕様IDと対応させて記述する。

| # | 確認内容 | 確認種別 | 対応SP番号 |
|---|----------|----------|-----------|
| 1 | {確認したい動作・条件} | 正常系／異常系／境界値 | SP-001 |

---

## 8. 気づき・提案メモ

> 作成・レビュー中に気づいた修正すべき内容・改善案・懸念事項を自由に記録する。  
> 現在のCRスコープ外の内容も記載可。次のCR起票・バックログの入力として活用する。

| # | 種別 | 内容 | 対応方針 |
|---|------|------|----------|
| 1 | 修正点／改善案／懸念／質問 | {内容} | 今回対応／次回CR／保留／却下 |

---

## 9. 変更履歴

| 版数 | 日付 | 変更者 | 変更内容 |
|------|------|--------|----------|
| 1.0 | {YYYY-MM-DD} | AI | 初版作成 |
| 1.1 | {YYYY-MM-DD} | AI | 不具合修正を反映（テストNGフィードバック） |
