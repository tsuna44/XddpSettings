# PLAN-20260504-close-reflects-ll-to-steering

作成日: 2026-05-04  
ステータス: 草案 / 承認待ち / 承認済み / **実装完了**

---

## 1. 背景・目的

現状、`project-steering.md` は人が手動で更新するファイルであり、
`xddp.close` が知識HUBに蓄積した知見（LL エントリ）は `project-steering.md` に自動反映されない。

CR クローズ時に今回 CR で得た技術パターン・禁止事項を `project-steering.md` に追記することで、
次 CR の工程05・06でより質の高いコンテキストを AI が参照できるようにする。

---

## 2. 変更対象ファイル

| ファイル | 変更種別 | 概要 |
|---|---|---|
| `ClaudeCode/.claude/skills/xddp.close.md` | 修正 | Step C3 の後に Step C3.5 を追加 |
| `ClaudeCode/.claude/commands/xddp.close.md` | 修正 | Step C3.5 の要約を追加 |

---

## 3. 変更内容

### 3.1. `ClaudeCode/.claude/skills/xddp.close.md`

**変更前（Step C3 直後）:**
```
## Step C4: 設計書の昇格（DSN・CHD → DOCS_DIR/{REPO_NAME}/design/）
```

**変更後（Step C3 と Step C4 の間に挿入）:**
```markdown
## Step C3.5: project-steering.md への知見反映

`{XDDP_DIR}/project-steering.md` が存在しない場合はスキップし、その旨を記録する。

Step C で今回 CR に追加した LL エントリ（**CR: {CR}** を含むもの）を対象とする。

以下の方針で `project-steering.md` の該当セクションへ追記する。

**セクションマッピング:**

| 対象タグ / 内容 | 追記先 |
|---|---|
| `#方式検討` `#設計` — 採用した設計パターン | Section 3（ADR）または Section 4 |
| `#コーディング` — 実装パターン・慣習 | Section 4（既存パターン・慣習）|
| `#テスト` — テストパターン | Section 4（テストパターン）|
| NG パターン・制約・禁止事項的な内容 | Section 5（禁止事項・注意事項）|
| `#プロセス` `#要求分析` `#仕様定義` | 反映対象外（技術パターンでないため）|

**追記ルール:**
- 同等のパターンがすでに記載されている場合は追記しない（重複チェック）
- 追記形式は既存セクション内の記述スタイルに合わせる（コードブロックまたは箇条書き）
- 各追記の末尾に `（出典: LL-{NNN}, {CR}）` をコメントとして付与する
- コードブロック内への挿入は避け、コードブロックの直下または次のコードブロックとして追記する

Section 7（変更履歴）に 1 行追記する:
```
| {TODAY} | {CR} | LL反映: {追記した LL-NNN 一覧} |
```

LL エントリが 1 件もマッピング対象でない場合（全エントリが `#プロセス` 等）はスキップし、
Section 7 に「反映対象 LL なし」と 1 行追記する。
```

**理由:** CR 完了時点で得た知見をその場で `project-steering.md` に追記することで、
次 CR 開始時から AI が最新パターンを参照できるようにする。

---

## 4. 影響範囲

- 影響するスキル・コマンド: `xddp.close`
- 影響する工程: CR クローズ（工程対象外・随時実行）
- 後方互換性: `project-steering.md` が存在しない場合はスキップするため既存プロジェクトへの影響なし

---

## 5. 確認項目

- [ ] スキル・コマンドの同期（`commands/xddp.close.md` も更新済み）
- [ ] `project-steering.md` が存在しないケースでスキップ動作することを確認
- [ ] 全 LL エントリが反映対象外タグのみの場合の動作確認
- [ ] CLAUDE.md の記述と整合

---

## 6. 承認

| 項目 | 内容 |
|---|---|
| 承認者 | |
| 承認日 | |
| 備考 | |
