# report-generator 制約・注意事項

> 更新ルール: 同一エントリは Upsert（[CK-NNN] で識別）。廃止エントリは削除可。出典は（CR-NNN）で記録。

## パフォーマンス・非機能特性

### [CK-001] レポート生成のメモリ展開
**観察:** src/report_generator.py:generate() はレポート生成が同期処理でデバイス全件をメモリに展開する。
**示唆:** ラベル絞り込み追加時はフィルタを先に適用しメモリ使用量を抑える設計が望ましい。
**出典:** CR-2026-900（SPO Section 5.6 より）
