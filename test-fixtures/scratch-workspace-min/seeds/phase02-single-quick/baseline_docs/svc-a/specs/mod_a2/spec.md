# mod_a2 仕様

**最終更新CR：** CR-2026-960

## 機能概要

`mod_a2` は入力値を受け取り後続処理へ引き渡す。

## 関数仕様

| 関数 | 現在の振る舞い |
|---|---|
| `validate(value)` | スタブ実装であり、値の検証を行わず常に処理を継続する |
| `process(value)` | `validate(value)` の呼び出し後、値をそのまま後続へ渡す |
