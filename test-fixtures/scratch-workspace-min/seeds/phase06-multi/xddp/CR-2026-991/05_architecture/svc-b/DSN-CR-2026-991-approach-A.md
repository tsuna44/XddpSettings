# 実装案A：送信直前に空文字列検証する方式

**文書番号：** DSN-CR-2026-991-approach-A
**作成日：** 2026-08-09
**作成者：** AI（フィクスチャ）

---

## 概要

`notify()` の先頭（svc-a への `POST /validate` 呼び出しより前）で空文字列検証を行い、
空文字列であれば例外を送出する。

## 設計方針

### 概略

`notify(value)` の先頭で `value` が空文字列でないことを確認する。svc-a への既存の
`POST /validate` 呼び出しはこのチェックの後段に置き、呼び出しタイミング・リクエスト形式は
変更しない（インタフェース契約自体は `../cross/DSN-CR-2026-991-cross.md` を参照）。

### 影響境界

`mod_b` 内部に閉じた変更。svc-a への既存呼び出しの形式・順序は変えない。

## メリット

- svc-a 側の変更が一切不要
- 既存のリポジトリ間呼び出しフローを変更しない

## デメリット

- （なし）

## 影響ファイル数

1件

---

## 採用理由

唯一の現実的な方式であるため。

## 変更設計書作成への指針

上記「設計方針」に従い CHD を作成する。

## 気づき・提案メモ

（なし）
