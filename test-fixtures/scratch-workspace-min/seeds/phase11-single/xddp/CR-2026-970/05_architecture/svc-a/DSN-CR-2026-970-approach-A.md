# 実装案A：呼び出し直前に範囲検証する方式

**文書番号：** DSN-CR-2026-970-approach-A
**作成日：** 2026-07-26
**作成者：** AI（xddp-architect-agent）

---

## 概要

`validate()` の先頭で範囲検証を行い、範囲外であれば例外を送出する。

## 設計方針

### 概略

`validate(value)` の先頭で `0 <= value <= 100` を確認する。

### 影響境界

`mod_a2` 内部に閉じた変更（single-repoのためcross境界なし）。

## メリット

- 呼び出し元の変更が不要

## デメリット

- （なし）

## 影響ファイル数

1件

---

## 採用理由

唯一の現実的な方式であるため。

## 変更設計書作成への指針

上記「設計方針」に従い CHD を作成する。

## 気づき・提案メモ

（なし）
