# 実装案A：呼び出し直前に範囲検証する方式

**文書番号：** DSN-CR-2026-961-approach-A
**作成日：** 2026-07-12
**作成者：** AI（フィクスチャ）

---

## 概要

`validate()` の直前で範囲検証を行い、範囲外であれば例外を送出する。

## 設計方針

### 概略

`validate(value)` の先頭で `0 <= value <= 100` を確認する。

### 影響境界

`mod_a2` 内部に閉じた変更（single-repoのためcross境界なし）。

## メリット

- 呼び出し元の変更が不要

## デメリット

- （なし）

## 影響ファイル数

1件

---

## 採用理由

唯一の現実的な方式であるため。

## 変更設計書作成への指針

`validate()` の先頭に範囲検証を追加する設計とする。

**非機能要件（検証用に意図的に追加した未反映項目）：** 範囲検証を含む `validate()` の応答時間は
平均1ms以内に収めること。この性能制約はCRS未反映であり、`/xddp.feedback CR-2026-961 arch` で
抽出されることを期待する検証用項目である。

## 気づき・提案メモ

（なし）
