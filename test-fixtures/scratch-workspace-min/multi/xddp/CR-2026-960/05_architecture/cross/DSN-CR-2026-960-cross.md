# 実装方式検討メモ（リポジトリ間インタフェース）

**文書番号：** DSN-CR-2026-960-cross
**対象CR：** CR-2026-960
**作成日：** 2026-07-12
**作成者：** AI（フィクスチャ）

---

## 概要

svc-a の検証結果を svc-b（notify）に伝える際のインタフェース方針。

## 設計方針

svc-a は検証失敗時に `mod_b.notify()` を呼び出さない（エラーを黙って握りつぶさず、呼び出し元に
例外として伝播させる）方式とする。

**インタフェース仕様（検証用に意図的に追加した未反映項目）：** svc-a→svc-b 間の `notify()` 呼び出しは、
検証済みの数値のみを渡すこととし、`notify(value)` の `value` は常に `int または float` であることを
契約とする。この契約はCRS未反映であり、`/xddp.feedback CR-2026-960 arch` で抽出されることを
期待する検証用項目である。

## 気づき・提案メモ

（なし）
